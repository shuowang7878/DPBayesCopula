% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DPCopula_helpers.R
\name{.pairwise_median_counts}
\alias{.pairwise_median_counts}
\title{Pairwise high–high counts using lexicographic tie-breaking}
\usage{
.pairwise_median_counts(data)
}
\arguments{
\item{data}{Numeric data.frame or matrix.}
}
\value{
Named numeric vector of pairwise high–high counts.
}
\description{
For each variable, an \eqn{n \times p} matrix of i.i.d. N(0,1) keys is drawn,
independent of the data. Observations are ordered lexicographically by
\code{(X[, j], key[, j])}. The first \code{ceiling(n/2)} rows are assigned
to the \code{"low"} group and the remainder to \code{"high"}. For every variable pair
\code{i < j}, the function counts the number of observations that fall into
the \code{"high"} group for both variables.
}
\keyword{internal}
